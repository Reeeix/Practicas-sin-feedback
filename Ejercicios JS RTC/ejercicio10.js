//? Calcular un promedio (media)
 
const numbers = [12, 21, 38, 5, 45, 37, 6];
function average(param) {
  let suma = 0;
  for (number of numbers) {
    suma += number;
  }
  let average = Math.round(suma / numbers.length);
  console.log(`La media de los números es ${average}`)
}

average();